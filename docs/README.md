【皮卡丘动画使用指南】
1. 用Visual Studio打开本项目文件夹。
2. 将顶部解决方案平台从“Win32”改为“x64”。
3. 按F7编译，按Ctrl+F5运行。

【重要：配置项目属性】
如果编译失败，请右键项目->属性，手动设置：
- 包含目录: $(ProjectDir)lib\include
- 库目录: $(ProjectDir)lib\lib\x64
- 字符集: 使用多字节字符集

【图片路径已更改】
代码中的图片路径已指向：可以修改
如果你的图片放错了位置，程序会无法加载。